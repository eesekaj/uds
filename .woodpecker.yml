matrix:
  RUST: [stable, nightly]

steps:
  - name: Linux_x86_64
    image: rust
    when:
      event: [push, pull_request]
    commands:
      - rustup default $RUST
      - cargo check
      - cargo test
      
